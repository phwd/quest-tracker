package com.oculus.ocms.locationservices.dumper;

import com.facebook.fab.dumper.base.DumperBaseModule;
import com.facebook.inject.AbstractLibraryModule;
import com.facebook.inject.AddToMultiBind;
import com.facebook.inject.AutoGeneratedBinder;
import com.facebook.inject.Binder;
import com.facebook.inject.BundledAndroidModule;
import com.facebook.inject.InjectorModule;
import com.facebook.stetho.dumpapp.DumperPlugin;
import com.facebook.ultralight.AutoGeneratedSwitchIdClass;
import com.facebook.ultralight.UL;
import com.google.inject.Key;
import com.oculus.ocms.locationservices.LocationServiceModule;
import com.oculus.time.TimeModule;

@InjectorModule
public abstract class LocationServiceDumperModule extends AbstractLibraryModule {

    @AutoGeneratedSwitchIdClass
    public static final class UL_id {
        public static final int _UL__ULSEP_com_oculus_ocms_locationservices_dumper_LocationServiceDumper_ULSEP_BINDING_ID = (UL.USE_STATIC_DI ? UL.id._UL__ULSEP_com_oculus_ocms_locationservices_dumper_LocationServiceDumper_ULSEP_BINDING_ID : UL.id.dynamicId(Key.get(LocationServiceDumper.class)));
    }

    /* access modifiers changed from: package-private */
    @AddToMultiBind
    public abstract DumperPlugin addAutoUpdatesDumperPlugin(LocationServiceDumper locationServiceDumper);

    @AutoGeneratedBinder
    static class AutoGeneratedBindingsForLocationServiceDumperModule {
        AutoGeneratedBindingsForLocationServiceDumperModule() {
        }

        static void bind(Binder binder) {
            if (!UL.USE_STATIC_DI) {
                binder.require(DumperBaseModule.class);
                binder.require(BundledAndroidModule.class);
                binder.require(LocationServiceModule.class);
                binder.require(TimeModule.class);
                binder.bindMulti(DumperPlugin.class).add(LocationServiceDumper.class);
                binder.bind(LocationServiceDumper.class).toProvider(new LocationServiceDumperAutoProvider());
            }
        }
    }
}
