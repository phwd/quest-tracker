package com.oculus.ocms.library.service;

import com.facebook.inject.AbstractLibraryModule;
import com.facebook.inject.AutoGeneratedBinder;
import com.facebook.inject.Binder;
import com.facebook.inject.BundledAndroidModule;
import com.facebook.inject.InjectorModule;
import com.facebook.mobileconfig.factory.module.MobileConfigFactoryModule;
import com.facebook.mobileconfig.interfaces.MobileConfigInterfacesModule;
import com.facebook.ultralight.AutoGeneratedSwitchIdClass;
import com.facebook.ultralight.UL;
import com.oculus.libraryapi.OVRLibraryModule;
import com.oculus.logging.utils.UtilsModule;
import com.oculus.security.basecomponent.BasecomponentModule;

@InjectorModule
public class ServiceModule extends AbstractLibraryModule {

    @AutoGeneratedSwitchIdClass
    public static final class UL_id {
    }

    @AutoGeneratedBinder
    static class AutoGeneratedBindingsForServiceModule {
        AutoGeneratedBindingsForServiceModule() {
        }

        static void bind(Binder binder) {
            if (!UL.USE_STATIC_DI) {
                binder.require(BundledAndroidModule.class);
                binder.require(MobileConfigFactoryModule.class);
                binder.require(MobileConfigInterfacesModule.class);
                binder.require(OVRLibraryModule.class);
                binder.require(UtilsModule.class);
                binder.require(BasecomponentModule.class);
                binder.bindComponent(BinaryCheckUpdateIntentService.class).toProvider(new BinaryCheckUpdateIntentServiceAutoProvider());
                binder.bindComponent(ThirdPartyPackageWarnOrBlockIntentService.class).toProvider(new ThirdPartyPackageWarnOrBlockIntentServiceAutoProvider());
            }
        }
    }
}
