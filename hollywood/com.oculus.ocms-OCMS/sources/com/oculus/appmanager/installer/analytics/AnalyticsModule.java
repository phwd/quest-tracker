package com.oculus.appmanager.installer.analytics;

import com.facebook.inject.AbstractLibraryModule;
import com.facebook.inject.ApplicationScoped;
import com.facebook.inject.AutoGeneratedBinder;
import com.facebook.inject.Binder;
import com.facebook.inject.BundledAndroidModule;
import com.facebook.inject.InjectorModule;
import com.facebook.ultralight.AutoGeneratedSwitchIdClass;
import com.facebook.ultralight.UL;
import com.google.inject.Key;
import com.oculus.appmanager.info.InfoModule;
import com.oculus.common.init.AppInitModule;
import com.oculus.common.packagescache.PackagesCacheModule;
import com.oculus.debug.DebugModule;
import com.oculus.logging.utils.UtilsModule;

@InjectorModule
public class AnalyticsModule extends AbstractLibraryModule {

    @AutoGeneratedSwitchIdClass
    public static final class UL_id {
        public static final int _UL__ULSEP_com_oculus_appmanager_installer_analytics_InstallerAnalytics_ULSEP_BINDING_ID = (UL.USE_STATIC_DI ? UL.id._UL__ULSEP_com_oculus_appmanager_installer_analytics_InstallerAnalytics_ULSEP_BINDING_ID : UL.id.dynamicId(Key.get(InstallerAnalytics.class)));
    }

    @AutoGeneratedBinder
    static class AutoGeneratedBindingsForAnalyticsModule {
        AutoGeneratedBindingsForAnalyticsModule() {
        }

        static void bind(Binder binder) {
            if (!UL.USE_STATIC_DI) {
                binder.require(BundledAndroidModule.class);
                binder.require(InfoModule.class);
                binder.require(AppInitModule.class);
                binder.require(PackagesCacheModule.class);
                binder.require(DebugModule.class);
                binder.require(UtilsModule.class);
                binder.bind(InstallerAnalytics.class).toProvider(new InstallerAnalyticsAutoProvider()).in(ApplicationScoped.class);
            }
        }
    }
}
