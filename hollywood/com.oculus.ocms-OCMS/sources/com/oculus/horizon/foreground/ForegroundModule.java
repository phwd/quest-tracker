package com.oculus.horizon.foreground;

import com.facebook.inject.AbstractLibraryModule;
import com.facebook.inject.ApplicationScoped;
import com.facebook.inject.AutoGeneratedBinder;
import com.facebook.inject.Binder;
import com.facebook.inject.BundledAndroidModule;
import com.facebook.inject.InjectorModule;
import com.facebook.ultralight.AutoGeneratedSwitchIdClass;
import com.facebook.ultralight.UL;
import com.google.inject.Key;
import com.oculus.time.TimeModule;
import com.oculus.util.task.TaskModule;

@InjectorModule
public class ForegroundModule extends AbstractLibraryModule {

    @AutoGeneratedSwitchIdClass
    public static final class UL_id {
        public static final int _UL__ULSEP_com_oculus_horizon_foreground_ApplicationForegroundTracker_ULSEP_BINDING_ID = (UL.USE_STATIC_DI ? UL.id._UL__ULSEP_com_oculus_horizon_foreground_ApplicationForegroundTracker_ULSEP_BINDING_ID : UL.id.dynamicId(Key.get(ApplicationForegroundTracker.class)));
    }

    @AutoGeneratedBinder
    static class AutoGeneratedBindingsForForegroundModule {
        AutoGeneratedBindingsForForegroundModule() {
        }

        static void bind(Binder binder) {
            if (!UL.USE_STATIC_DI) {
                binder.require(BundledAndroidModule.class);
                binder.require(TimeModule.class);
                binder.require(TaskModule.class);
                binder.bind(ApplicationForegroundTracker.class).toProvider(new ApplicationForegroundTrackerAutoProvider()).in(ApplicationScoped.class);
            }
        }
    }
}
