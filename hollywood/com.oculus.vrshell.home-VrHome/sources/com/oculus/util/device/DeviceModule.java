package com.oculus.util.device;

import com.facebook.inject.AbstractLibraryModule;
import com.facebook.inject.Binder;
import com.facebook.inject.BundledAndroidModule;
import com.facebook.ultralight.UL;
import com.google.inject.Key;

public class DeviceModule extends AbstractLibraryModule {

    public static final class UL_id {
        public static final int $ul_$xXXcom_oculus_util_device_DeviceUtils$xXXBINDING_ID = (UL.USE_STATIC_DI ? UL.id.$ul_$xXXcom_oculus_util_device_DeviceUtils$xXXBINDING_ID : UL.id.dynamicId(Key.get(DeviceUtils.class)));
    }

    static class AutoGeneratedBindingsForDeviceModule {
        AutoGeneratedBindingsForDeviceModule() {
        }

        static void bind(Binder binder) {
            if (!UL.USE_STATIC_DI) {
                binder.require(BundledAndroidModule.class);
                binder.bind(DeviceUtils.class).toProvider(new DeviceUtilsAutoProvider());
            }
        }
    }
}
