package com.oculus.util.thread;

import X.AbstractC06640p5;
import X.AnonymousClass117;
import android.os.Looper;
import com.facebook.ultralight.AutoGeneratedAccessMethod;
import com.facebook.ultralight.AutoGeneratedFactoryMethod;
import com.facebook.ultralight.Dependencies;

@Dependencies({})
public class ThreadUtils {
    public static final String CANT_BE_UI_THREAD = "This operation must not be run on UI thread.";
    public static final String MUST_BE_UI_THREAD = "This operation must be run on UI thread.";

    @AutoGeneratedFactoryMethod
    public static final ThreadUtils A00() {
        return new ThreadUtils();
    }

    @AutoGeneratedAccessMethod
    public static final ThreadUtils A01(AbstractC06640p5 r1) {
        return (ThreadUtils) AnonymousClass117.A00(583, r1);
    }

    @Deprecated
    public static void A02() {
        if (!(!A04())) {
            throw new IllegalStateException(CANT_BE_UI_THREAD);
        }
    }

    public final void A05() {
        A03(CANT_BE_UI_THREAD);
    }

    public static final void A03(String str) {
        if (!(!A04())) {
            throw new IllegalStateException(str);
        }
    }

    public static boolean A04() {
        if (Looper.getMainLooper().getThread() == Thread.currentThread()) {
            return true;
        }
        return false;
    }
}
